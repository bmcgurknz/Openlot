## What does this change?

<!-- One or two sentences. Link the issue if there is one. -->

## Why?

## How was it tested?

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (new behaviour has tests)
- [ ] `cd web && npm run build` passes (if the UI changed)
- [ ] Docs updated (if behaviour or configuration changed)

## Conformance-rule changes

If this PR touches `src/services/conformance.ts` or `src/services/claims.ts`,
explain the contractual/spec basis for the rule change (these rules exist to
substantiate progress claims — they are not ordinary code):
